# Prevent native module compilation (better-sqlite3 / sqlite3 / node-gyp).
# Production on Vercel uses Turso only; local SQLite packages are unused there.
# This also wins if the Vercel dashboard overrides installCommand.
ignore-scripts=true
